[Paths]
flag_dir = flags
# data_dir = data ; Not used yet
# results_dir = results ; Not used yet

[Flags]
data_ready = data_ready.flag
results_ready = results_ready.flag

[Timing]
# How often the collector runs its cycle (seconds)
collector_interval_seconds = 15
processor_interval_seconds = 30
communicator_interval_seconds = 60
# How often processes check for flags when waiting (seconds)
poll_interval_seconds = 2